const gameBoard = (() => {
    const tiles = ['X', 'X', 'X', 'X', 'X', 'X', 'X', 'X', 'X',];
    //render gameboard to HTML on window load
}

const player = ((name) => {
    //set player name to name value from input
}